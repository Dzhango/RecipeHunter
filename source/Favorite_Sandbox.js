var currentRecipe = a; // the recipe that the user is currently looning at.

//get storage
myStorage = window.localStorage;
//get list of favorite recipes
let favoriteRecipes = localStorage.getItem('favorite_recipes'); // This is a string
function storeIntoFav(obj){  // Translate string to an array? Can we directly store and get an array object?
    let stored = false;
    
}

//